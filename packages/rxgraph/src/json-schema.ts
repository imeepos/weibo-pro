
import { JSONSchema6 as JsonSchema } from 'json-schema';
import { RxNode } from './types';
export class JsonSchemaBuilder {
    build(node: RxNode): JsonSchema {
        throw new Error(`not implement`)
    }
}