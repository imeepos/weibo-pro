/**
 * @sker/json-harmony
 * 在混沌中寻找和谐：优雅地解析损坏的 JSON
 *
 * 存在即合理 - 每一行代码都有其不可替代的目的
 * 优雅即简约 - 代码自述其意，无需注释喧哗
 */

export { JsonHarmonyParser, parse } from './parser'
export type { ParseResult, ParseStatistics, ParserConfig } from './types'
export { RecoveryStrategy } from './types'
