/**
 * 错误处理共享实现，统一由 @sker/workflow 提供。
 * 保留此文件仅为兼容既有相对路径 import，勿再在此维护副本。
 */
export {
  ErrorCategory,
  ErrorClassifier,
  ErrorFormatter,
  ErrorHandlerOperators,
  type ErrorHandlerConfig,
} from '@sker/workflow';
