# Hero 布局技巧深度解析

## 📚 目录

1. [整体架构](#整体架构)
2. [核心技术拆解](#核心技术拆解)
3. [CSS 类详解](#css-类详解)
4. [响应式策略](#响应式策略)
5. [性能优化](#性能优化)
6. [可视化图解](#可视化图解)
7. [实战技巧](#实战技巧)

---

## 整体架构

### 层次结构图

```
┌─────────────────────────────────────────────────────────────┐
│ Hero Container (relative isolate overflow-hidden)          │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Layer 1: 电路板装饰层 (absolute inset-0 -z-10)         │ │
│ │ ├── 左侧电路板图片                                      │ │
│ │ ├── 右侧电路板图片（镜像）                              │ │
│ │ ├── Canvas 动画连接线                                   │ │
│ │ └── SVG 路径定义                                        │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Layer 2: 径向渐变遮罩 (absolute -z-10)                 │ │
│ │ bg-[radial-gradient(...)]                              │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Layer 3: 彩色渐变 SVG (absolute -z-10)                 │ │
│ │ mix-blend-color-burn                                   │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Layer 4: 内容层 (z-index: auto)                        │ │
│ │ ├── 主标题 (h1)                                         │ │
│ │ ├── 描述文本 (p)                                        │ │
│ │ └── CTA 按钮组 (div)                                    │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 关键设计原则

1. **层叠上下文隔离**：使用 `isolate` 创建独立渲染上下文
2. **装饰层后置**：所有背景元素使用 `-z-10` 确保在内容下方
3. **对称美学**：通过镜像变换减少资源加载
4. **渐进增强**：从基础布局到高级视觉效果逐层叠加

---

## 核心技术拆解

### 1. 层叠上下文管理

#### 技术原理

```css
/* 父容器 */
.hero-container {
  position: relative;
  isolation: isolate;  /* 关键属性 */
  overflow: hidden;
}

/* 背景层 */
.background-layer {
  position: absolute;
  z-index: -10;  /* 确保在内容下方 */
}
```

#### 为什么使用 `isolate`？

**问题场景：**
```html
<!-- 没有 isolate 的情况 -->
<div class="parent relative">
  <div class="bg absolute z-[-10]"></div>
  <div class="content">
    <button class="z-50">按钮</button>
  </div>
</div>
<div class="external z-40">外部元素</div>
```

- 如果没有 `isolate`，按钮的 `z-50` 会与外部元素的 `z-40` 比较
- 可能导致层级混乱

**使用 isolate 后：**
```html
<div class="parent relative isolate">  <!-- 创建独立层叠上下文 -->
  <div class="bg absolute z-[-10]"></div>
  <button class="z-50">按钮</button>
</div>
<div class="external z-40">外部元素</div>
```

- `isolate` 创建独立层叠上下文
- 内部的 `z-index` 只在容器内部比较
- 不会影响外部元素

#### TailwindCSS 实现

```jsx
<div className="relative isolate overflow-hidden">
  {/* 所有子元素的 z-index 被隔离 */}
</div>
```

---

### 2. 对称镜像布局

#### 技术实现

```jsx
{/* 左侧原始图片 */}
<div className="absolute -top-8 right-1/2 sm:top-5 aspect-[969/887] w-[969px]">
  <picture>
    <source srcSet="circuit-lines@2xl.webp" type="image/webp" />
    <source srcSet="circuit-lines@2xr.png" type="image/png" />
    <img src="circuit-lines@2xl.webp" className="absolute inset-0 h-full w-full" />
  </picture>
</div>

{/* 右侧镜像图片 - 关键：origin-right + -scale-x-100 */}
<div className="absolute -top-8 right-1/2 origin-right -scale-x-100 sm:top-5 aspect-[969/887] w-[969px]">
  <picture>
    {/* 使用相同的图片资源 */}
    <img src="circuit-lines@2xl.webp" className="absolute inset-0 h-full w-full" />
  </picture>
</div>
```

#### 关键属性解析

| 属性 | 作用 | 效果 |
|------|------|------|
| `right-1/2` | 定位到容器右侧 50% 处 | 两侧图片从中心向外延伸 |
| `origin-right` | 设置变换原点为右侧 | 围绕右边缘翻转（而非中心）|
| `-scale-x-100` | 水平翻转 100% | 创建镜像效果 |
| `aspect-[969/887]` | 固定宽高比 | 保持图片不变形 |

#### 变换原点可视化

```
原始图片（左侧）:
┌──────────────────┐
│                  │
│  [内容]          │
│                  │
└──────────────────┘
  ^
  └─ right: 50% (从右侧 50% 定位)

镜像图片（右侧）:
                   ┌──────────────────┐
                   │                  │
origin-right ──►   │          [容内]  │  ◄── scale-x: -1
                   │                  │
                   └──────────────────┘
                     ^
                     └─ right: 50% (同样从右侧 50% 定位)
```

#### 为什么这样做？

**优势：**
1. **节省资源**：只需加载一张图片，减少 HTTP 请求
2. **一致性**：左右两侧完全对称，避免视觉差异
3. **易维护**：更新图片时只需替换一个文件

**对比传统方法：**
```jsx
// ❌ 传统方法：需要两张图片
<img src="left-circuit.png" />
<img src="right-circuit.png" />

// ✅ 镜像方法：一张图片
<img src="circuit.png" />
<img src="circuit.png" className="-scale-x-100" />
```

---

### 3. 渐变遮罩技术

#### 径向渐变实现

```jsx
<div className="
  absolute inset-x-0
  bottom-[calc(-702/16*1rem)] top-0
  -z-10
  bg-[radial-gradient(
    154.86%_76.83%_at_50%_22.26%,
    theme(colors.gray.50/0.4)_8.98%,
    theme(colors.gray.50)_45.99%
  )]
">
</div>
```

#### 参数详解

```css
radial-gradient(
  154.86% 76.83%          /* 椭圆半径：宽度 154.86%，高度 76.83% */
  at 50% 22.26%,          /* 圆心位置：水平 50%（居中），垂直 22.26%（偏上）*/
  gray-50/0.4 8.98%,      /* 起始色：40% 不透明度，占 8.98% */
  gray-50 45.99%          /* 结束色：100% 不透明度，占 45.99% */
)
```

#### 可视化效果

```
顶部（22.26%）
    ↓
    ●  ← 圆心位置
   ╱ ╲
  ╱   ╲   8.98% - 起始色（40% 透明）
 ╱     ╲
│       │
│       │ 45.99% - 结束色（100% 不透明）
│       │
└───────┘
底部
```

#### 位置计算技巧

```jsx
bottom-[calc(-702/16*1rem)]
// = -43.875rem = -702px
// 负值让渐变延伸到容器底部之外，创造柔和过渡
```

**为什么用 `calc(-702/16*1rem)` 而不是 `-702px`？**

1. **响应式**：`rem` 单位随根字体大小变化
2. **可维护**：设计稿是 702px，通过 `/16` 转换为 rem
3. **精确性**：避免浏览器默认字体大小差异

#### 在您的项目中应用

```jsx
// 方案 1：使用 TailwindCSS 任意值
<div className="bg-[radial-gradient(
  150%_80%_at_50%_20%,
  rgb(var(--color-primary)/0.3)_10%,
  rgb(var(--color-background))_50%
)]" />

// 方案 2：使用 CSS 变量（推荐）
// 在 index.css 中定义
@layer utilities {
  .radial-overlay {
    background: radial-gradient(
      150% 80% at 50% 20%,
      rgb(var(--color-primary) / 0.3) 10%,
      rgb(var(--color-background)) 50%
    );
  }
}
```

---

### 4. SVG 滤镜与混合模式

#### 完整实现

```jsx
<svg
  viewBox="-1000 0 3504 918"
  className="
    absolute -top-6 left-1/2 -z-10
    ml-[calc(-3504/2/16*1rem)]        /* 居中对齐 */
    w-[calc(3504/16*1rem)]            /* 宽度转换 */
    mix-blend-color-burn              /* 颜色燃烧混合 */
  "
>
  <path fill="url(#hero-gradient)" d="M3504 918H-1000V0h3504v918Z" />

  <defs>
    <radialGradient
      id="hero-gradient"
      cx="0" cy="0" r="1"
      gradientTransform="matrix(0 707.279 -1739.2 0 741 159.991)"
      gradientUnits="userSpaceOnUse"
    >
      <stop stop-color="#6C47FF" stop-opacity="0.6" />         {/* 紫色 */}
      <stop offset=".412" stop-color="#FFF963" stop-opacity=".8" /> {/* 黄色 */}
      <stop offset=".623" stop-color="#38DAFD" stop-opacity=".6" /> {/* 青色 */}
      <stop offset=".919" stop-color="#6248F6" stop-opacity="0" />  {/* 透明 */}
    </radialGradient>
  </defs>
</svg>
```

#### 混合模式详解

**`mix-blend-color-burn` 效果：**

```
原理：将背景颜色反转后与前景颜色相乘，产生更深的颜色
公式：Result = 1 - ((1 - Background) / Foreground)

视觉效果：
- 白色背景 + 彩色渐变 → 颜色加深、对比增强
- 暗色背景 + 彩色渐变 → 颜色融合、高级感
```

**常用混合模式对比：**

| 混合模式 | 效果 | 适用场景 |
|---------|------|---------|
| `multiply` | 正片叠底，颜色加深 | 阴影效果 |
| `screen` | 滤色，颜色变亮 | 光晕效果 |
| `overlay` | 叠加，保留高光和阴影 | 纹理叠加 |
| `color-burn` | 颜色燃烧，增加对比 | 渐变融合 |
| `soft-light` | 柔光，柔和叠加 | 背景装饰 |

#### 渐变变换矩阵

```jsx
gradientTransform="matrix(0 707.279 -1739.2 0 741 159.991)"
```

**矩阵参数：**
```
matrix(a, b, c, d, e, f)
a c e     0       741
b d f  =  707.279 159.991
0 0 1     -1739.2 0
```

**作用：**
- 旋转渐变方向
- 缩放渐变椭圆
- 偏移渐变中心

#### 颜色停止点策略

```jsx
<stop offset="0"     stop-color="#6C47FF" stop-opacity="0.6" />  // 起始：紫色
<stop offset=".412"  stop-color="#FFF963" stop-opacity=".8" />   // 41%：黄色（峰值）
<stop offset=".623"  stop-color="#38DAFD" stop-opacity=".6" />   // 62%：青色
<stop offset=".919"  stop-color="#6248F6" stop-opacity="0" />    // 92%：透明
```

**不均匀分布的意义：**

```
0%        41%       62%       92%      100%
│─────────│─────────│─────────│─────────│
紫        黄        青        透明
     较慢过渡    较快过渡    超慢过渡
```

- **0-41%**：紫色到黄色，较慢过渡，创造丰富色彩
- **41-62%**：黄色到青色，较快过渡，形成视觉焦点
- **62-92%**：青色到透明，超慢过渡，边缘柔和消失

---

### 5. 响应式排版系统

#### 多级断点策略

```jsx
<h1 className="
  mx-auto
  max-w-[17.5rem] text-3xl/9               /* 基础：移动端 */
  sm:!max-w-4xl sm:text-4.5xl               /* 640px+ */
  md:text-5xl md:font-bold                  /* 768px+ */
  lg:text-6xl                               /* 1024px+ */
  [@media(width>=27.5em)]:max-w-[24.25rem] /* 自定义断点 440px+ */
">
```

#### 字体大小映射表

| 类名 | 字体大小 | 行高 | 适用场景 |
|------|---------|------|---------|
| `text-3xl/9` | 1.875rem (30px) | 2.25rem (36px) | 移动端主标题 |
| `text-4.5xl` | 2.5rem (40px) | 1 | 小屏幕 |
| `text-5xl` | 3rem (48px) | 1 | 中屏幕 |
| `text-6xl` | 3.75rem (60px) | 1 | 大屏幕 |

#### 自定义媒体查询

```jsx
[@media(width>=27.5em)]:max-w-[24.25rem]
// TailwindCSS v3.2+ 任意媒体查询语法
// width >= 27.5em = 440px
```

**为什么用 `em` 而不是 `px`？**

- `em` 基于用户浏览器字体设置
- 更好的可访问性支持
- 在用户放大字体时自动调整断点

#### 容器宽度策略

```
移动端（< 440px）:
┌───────────────────┐
│   max-w-[17.5rem] │  280px
│   (较窄，适合长标题换行)
└───────────────────┘

小屏（440px - 640px）:
┌─────────────────────────┐
│   max-w-[24.25rem]      │  388px
└─────────────────────────┘

中屏（640px+）:
┌───────────────────────────────────┐
│   max-w-4xl (56rem = 896px)      │
└───────────────────────────────────┘
```

#### 在您的项目中应用

```jsx
// 方案 1：使用 TailwindCSS 配置扩展
// tailwind.config.js
export default {
  theme: {
    extend: {
      fontSize: {
        '4.5xl': '2.5rem',
        'hero-mobile': ['1.875rem', { lineHeight: '2.25rem' }],
        'hero-desktop': ['3.75rem', { lineHeight: '1.2' }],
      }
    }
  }
}

// 使用
<h1 className="text-hero-mobile lg:text-hero-desktop">标题</h1>

// 方案 2：使用 CSS clamp（流式排版）
// index.css
.hero-title {
  font-size: clamp(1.875rem, 2vw + 1rem, 3.75rem);
  /* 最小 30px，理想 2vw + 16px，最大 60px */
}
```

---

### 6. 按钮高级效果

#### 多层伪元素结构

```jsx
<a className="
  group relative isolate                     /* 1. 组和层叠上下文 */
  inline-flex items-center justify-center    /* 2. 弹性布局 */

  /* 3. 过渡动画 */
  transition duration-300 ease-[cubic-bezier(0.4,0.36,0,1)]

  /* 4. 第一层伪元素（白色渐变） */
  before:pointer-events-none before:absolute before:inset-0 before:-z-10
  before:rounded-md before:bg-gradient-to-b before:from-white/20
  before:opacity-50
  before:duration-300 before:ease-[cubic-bezier(0.4,0.36,0,1)]
  before:transition-opacity
  hover:before:opacity-100

  /* 5. 第二层伪元素（叠加混合） */
  after:pointer-events-none after:absolute after:inset-0 after:-z-10
  after:rounded-md after:bg-gradient-to-b
  after:from-white/10 after:from-[46%] after:to-[54%]
  after:mix-blend-overlay

  /* 6. 基础样式 */
  rounded-md
  shadow-[0_1px_theme(colors.white/0.07)_inset,0_1px_3px_theme(colors.gray.900/0.2)]
  text-sm h-[1.875rem] px-3
  ring-1 bg-purple-500 text-white ring-purple-500
">
  Start building for free
</a>
```

#### 层次结构解析

```
按钮层次（从下到上）:
├── 5. 背景色（bg-purple-500）
├── 4. after 伪元素（mix-blend-overlay）
├── 3. before 伪元素（白色渐变，悬停透明度变化）
├── 2. 描边（ring-1）
├── 1. 内容（文字）
└── 0. 阴影（shadow）
```

#### 缓动函数详解

```css
ease-[cubic-bezier(0.4, 0.36, 0, 1)]
```

**贝塞尔曲线可视化：**

```
速度
 ↑
 │     ╱──────
 │   ╱
 │ ╱
 │╱
 └──────────→ 时间
 0   0.4  1

参数说明：
- (0.4, 0.36)：第一个控制点，缓慢启动
- (0, 1)：第二个控制点，快速结束
- 效果：先慢后快，优雅流畅
```

**对比常用缓动：**

| 缓动函数 | 效果 | 适用场景 |
|---------|------|---------|
| `ease` | 慢-快-慢 | 通用动画 |
| `ease-in` | 慢启动 | 元素消失 |
| `ease-out` | 慢结束 | 元素出现 |
| `ease-in-out` | 慢-快-慢（对称）| 循环动画 |
| `cubic-bezier(0.4,0.36,0,1)` | 慢启动-超快结束 | 按钮悬停 |

#### 阴影系统

```css
shadow-[
  0_1px_theme(colors.white/0.07)_inset,    /* 内阴影：顶部高光 */
  0_1px_3px_theme(colors.gray.900/0.2)     /* 外阴影：底部阴影 */
]
```

**可视化效果：**

```
┌────────────────────┐
│ ← 内阴影（白色高光）
│                    │
│   Button Text      │
│                    │
└────────────────────┘
  ↓ 外阴影（深色阴影）
```

#### 渐变停止点精确控制

```css
after:from-white/10 after:from-[46%] after:to-[54%]
```

**渐变分布：**

```
0%        46%       54%      100%
│─────────│─────────│─────────│
透明       白色10%    透明
      快速上升   快速下降
```

- **0-46%**：从透明到白色 10%
- **46-54%**：保持白色 10%（8% 的高光带）
- **54-100%**：从白色 10% 到透明

**为什么是 46-54%？**
- 创造细窄的高光带
- 位置接近中心，形成玻璃质感
- 与 `mix-blend-overlay` 配合，增强立体感

#### 在您的项目中应用

```jsx
// 扩展现有的 glass-card 样式
// apps/bigscreen/src/styles/index.css

@layer components {
  .glass-button {
    @apply
      group relative isolate
      inline-flex items-center justify-center
      px-4 py-2 rounded-lg
      bg-primary/80 text-white
      ring-1 ring-primary
      transition-all duration-300

      before:absolute before:inset-0 before:-z-10
      before:rounded-lg before:bg-gradient-to-b
      before:from-white/20 before:opacity-0
      before:transition-opacity before:duration-300
      hover:before:opacity-100

      after:absolute after:inset-0 after:-z-10
      after:rounded-lg after:bg-gradient-to-b
      after:from-white/10 after:from-[46%] after:to-[54%]
      after:mix-blend-overlay

      shadow-[0_1px_rgb(255_255_255/0.1)_inset,0_2px_8px_rgb(0_0_0/0.2)];
  }

  .glass-button:hover {
    @apply -translate-y-0.5 shadow-[0_1px_rgb(255_255_255/0.1)_inset,0_4px_12px_rgb(0_0_0/0.3)];
  }
}
```

---

### 7. Canvas 动画与 SVG 路径

#### Canvas 定位策略

```jsx
<canvas
  className="absolute inset-0 h-full w-full"
  aria-hidden="true"
  style={{
    left: 'calc(31.5rem)',      /* 504px */
    top: 'calc(1.5625rem)',     /* 25px */
    width: 'calc(25.1875rem)',  /* 403px */
    height: 'calc(22.6875rem)'  /* 363px */
  }}
  width="403"   /* 物理像素 */
  height="363"  /* 物理像素 */
/>
```

#### 关键属性对比

| 属性 | 作用 | 示例 |
|------|------|------|
| `style.left/top` | CSS 定位（视觉位置）| `left: 504px` |
| `style.width/height` | CSS 尺寸（显示大小）| `width: 403px` |
| `width/height` 属性 | Canvas 分辨率（绘图精度）| `width="403"` |

**为什么需要两套尺寸？**

```jsx
// ❌ 只设置 CSS 尺寸 - 图像模糊
<canvas style={{ width: '403px', height: '363px' }} />
// 默认 300x150 分辨率被拉伸到 403x363，导致模糊

// ✅ 同时设置分辨率和尺寸 - 图像清晰
<canvas
  width="403" height="363"  {/* 分辨率 */}
  style={{ width: '403px', height: '363px' }}  {/* 显示尺寸 */}
/>

// 🎯 高清屏优化
<canvas
  width="806" height="726"  {/* 2x 分辨率 */}
  style={{ width: '403px', height: '363px' }}  {/* 显示尺寸 */}
/>
```

#### SVG 路径定义

```jsx
<svg width="0" height="0" aria-hidden="true">
  <path d="M296 224V187.314C296 185.192 296.843 183.157 298.343 181.657L352.657 127.343C354.157 125.843 356.192 125 358.314 125H383" />
</svg>
```

**路径命令解析：**

```
M296 224           移动到 (296, 224)
V187.314           垂直线到 y=187.314
C296 185.192 ...   贝塞尔曲线（圆角）
L352.657 127.343   直线到 (352.657, 127.343)
C354.157 ...       贝塞尔曲线（圆角）
H383               水平线到 x=383
```

**可视化路径：**

```
(383, 125) ●────────┐
                    │ 圆角
                    ├─────────● (352.657, 127.343)
                    │
                    │ 直线段
                    │
                    ├─────────● (296, 187.314)
                    │ 圆角
                    │
                    ● (296, 224)
```

#### 为什么 SVG 设置 `width="0" height="0"`？

```jsx
// 这是一个路径定义容器，不需要显示
<svg width="0" height="0" aria-hidden="true">
  <path id="circuit-path-1" d="..." />
  <path id="circuit-path-2" d="..." />
</svg>

// 在 Canvas 或其他 SVG 中引用这些路径
<canvas ref={canvasRef} />

// 在 Canvas 中绘制 SVG 路径
const path = new Path2D('M296 224V187.314...');
ctx.stroke(path);
```

#### 动画实现示例

```typescript
// 电路板连接线动画
interface CircuitLine {
  path: Path2D;
  progress: number;
  speed: number;
}

function animateCircuit(canvas: HTMLCanvasElement) {
  const ctx = canvas.getContext('2d')!;
  const lines: CircuitLine[] = [
    {
      path: new Path2D('M296 224V187.314C296 185.192...'),
      progress: 0,
      speed: 0.01
    },
    // ... 更多路径
  ];

  function draw() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);

    lines.forEach(line => {
      // 绘制路径
      ctx.strokeStyle = '#6C47FF';
      ctx.lineWidth = 2;
      ctx.setLineDash([5, 5]);
      ctx.lineDashOffset = -line.progress * 100;
      ctx.stroke(line.path);

      // 更新进度
      line.progress += line.speed;
      if (line.progress > 1) line.progress = 0;
    });

    requestAnimationFrame(draw);
  }

  draw();
}
```

---

## CSS 类详解

### 容器类

```jsx
className="
  relative              /* 相对定位，为子元素提供定位上下文 */
  isolate               /* 创建独立层叠上下文 */
  overflow-hidden       /* 隐藏溢出内容（背景装饰）*/
  bg-gray-50            /* 基础背景色 */
  pb-20 pt-[calc(...)]  /* 内边距（动态计算）*/
  sm:pb-32 sm:pt-[...] /* 响应式内边距 */
  -mt-16                /* 负外边距（与上方元素重叠）*/
"
```

### 背景层类

```jsx
className="
  absolute              /* 绝对定位 */
  inset-0               /* top:0 right:0 bottom:0 left:0 */
  -z-10                 /* 层级低于内容 */
"
```

### 图片容器类

```jsx
className="
  absolute              /* 绝对定位 */
  -top-8                /* 向上偏移 32px（溢出容器）*/
  right-1/2             /* 右侧 50%（从中心向外）*/
  sm:top-5              /* 小屏幕向下偏移 20px */
  aspect-[969/887]      /* 宽高比 969:887 */
  w-[969px]             /* 固定宽度 969px */
"
```

### 镜像类

```jsx
className="
  origin-right          /* 变换原点为右侧 */
  -scale-x-100          /* 水平翻转（scaleX: -1）*/
"
```

### 渐变遮罩类

```jsx
className="
  absolute
  inset-x-0                        /* 水平方向填满 */
  bottom-[calc(-702/16*1rem)]      /* 底部负值延伸 */
  top-0
  -z-10
  bg-[radial-gradient(...)]        /* 径向渐变 */
"
```

### SVG 类

```jsx
className="
  absolute
  -top-6                           /* 向上偏移 24px */
  left-1/2                         /* 左侧 50% */
  -z-10
  ml-[calc(-3504/2/16*1rem)]       /* 负左外边距居中 */
  w-[calc(3504/16*1rem)]           /* 宽度 219rem */
  mix-blend-color-burn             /* 颜色燃烧混合 */
"
```

### 内容容器类

```jsx
className="
  text-center                      /* 文本居中 */
  mx-auto                          /* 水平居中 */
  w-full
  px-6                             /* 水平内边距 24px */
  sm:max-w-[40rem]                 /* 小屏最大宽度 640px */
  md:max-w-[48rem]                 /* 中屏最大宽度 768px */
  md:px-8                          /* 中屏内边距 32px */
  lg:max-w-[64rem]                 /* 大屏最大宽度 1024px */
  xl:max-w-[80rem]                 /* 超大屏最大宽度 1280px */
"
```

### 标题类

```jsx
className="
  mx-auto                          /* 水平居中 */
  max-w-[17.5rem]                  /* 基础最大宽度 280px */
  text-balance                     /* 文本平衡（防止孤行）*/
  text-3xl/9                       /* 字体 30px，行高 36px */
  font-bold                        /* 字重 700 */
  tracking-tight                   /* 字间距紧凑 */
  text-gray-950                    /* 文字颜色 */
  sm:!max-w-4xl                    /* 小屏强制最大宽度 896px */
  sm:text-4.5xl                    /* 小屏字体 40px */
  md:text-5xl                      /* 中屏字体 48px */
  md:font-bold                     /* 中屏字重 700 */
  lg:text-6xl                      /* 大屏字体 60px */
  [@media(width>=27.5em)]:max-w-[24.25rem] /* 自定义断点 */
"
```

### 描述文本类

```jsx
className="
  mx-auto                          /* 水平居中 */
  mt-4                             /* 顶部外边距 16px */
  max-w-md                         /* 最大宽度 28rem (448px) */
  text-pretty                      /* 文本优化渲染 */
  text-base/6                      /* 字体 16px，行高 24px */
  text-gray-600                    /* 文字颜色 */
  sm:max-w-2xl                     /* 小屏最大宽度 42rem (672px) */
  sm:text-lg                       /* 小屏字体 18px */
"
```

### 按钮容器类

```jsx
className="
  mt-8                             /* 顶部外边距 32px */
  flex                             /* 弹性布局 */
  items-center                     /* 垂直居中 */
  justify-center                   /* 水平居中 */
  gap-x-6                          /* 水平间隔 24px */
  gap-y-3                          /* 垂直间隔 12px */
  max-sm:flex-col                  /* 小于 640px 垂直排列 */
"
```

### 按钮类（完整）

```jsx
className="
  group                            /* 悬停组 */
  relative                         /* 相对定位 */
  isolate                          /* 层叠上下文 */
  inline-flex                      /* 行内弹性 */
  items-center                     /* 垂直居中 */
  justify-center                   /* 水平居中 */
  overflow-hidden                  /* 隐藏溢出 */
  text-left                        /* 文本左对齐 */
  font-medium                      /* 字重 500 */

  /* 过渡动画 */
  transition duration-300
  ease-[cubic-bezier(0.4,0.36,0,1)]

  /* before 伪元素 */
  before:duration-300
  before:ease-[cubic-bezier(0.4,0.36,0,1)]
  before:transition-opacity
  before:pointer-events-none
  before:absolute before:inset-0 before:-z-10
  before:rounded-md
  before:bg-gradient-to-b before:from-white/20
  before:opacity-50
  hover:before:opacity-100

  /* after 伪元素 */
  after:pointer-events-none
  after:absolute after:inset-0 after:-z-10
  after:rounded-md
  after:bg-gradient-to-b
  after:from-white/10 after:from-[46%] after:to-[54%]
  after:mix-blend-overlay

  /* 基础样式 */
  rounded-md
  shadow-[0_1px_theme(colors.white/0.07)_inset,0_1px_3px_theme(colors.gray.900/0.2)]
  text-sm                          /* 字体 14px */
  h-[1.875rem]                     /* 高度 30px */
  px-3                             /* 水平内边距 12px */
  ring-1                           /* 描边宽度 1px */
  bg-purple-500                    /* 背景色 */
  text-white                       /* 文字颜色 */
  ring-purple-500                  /* 描边颜色 */
"
```

---

## 响应式策略

### 断点系统

| 断点 | 最小宽度 | 适用设备 | 布局调整 |
|------|---------|---------|---------|
| `default` | 0px | 移动设备 | 单列布局，较小字体 |
| `sm` | 640px | 大手机/小平板 | 字体放大，容器宽度增加 |
| `md` | 768px | 平板 | 两列布局，字体进一步放大 |
| `lg` | 1024px | 小笔记本 | 最大字体，多列布局 |
| `xl` | 1280px | 台式机 | 最大容器宽度 |
| `2xl` | 1536px | 大屏幕 | - |

### 响应式图片

```jsx
<picture>
  <source srcSet="circuit-lines@2xl.webp" type="image/webp" />
  <source srcSet="circuit-lines@2xr.png" type="image/png" />
  <img
    alt=""
    width="1938"
    height="1774"
    decoding="async"
    className="absolute inset-0 h-full w-full"
    src="circuit-lines@2xl.webp"
  />
</picture>
```

**关键属性：**

| 属性 | 作用 |
|------|------|
| `<source type="image/webp">` | 支持 WebP 的浏览器优先使用 |
| `<source type="image/png">` | 不支持 WebP 时降级到 PNG |
| `decoding="async"` | 异步解码，不阻塞页面渲染 |
| `width/height` 属性 | 预留布局空间，防止 CLS |

### 响应式间距

```jsx
// 内边距
pb-20              // 移动端：80px
sm:pb-32           // 小屏：128px

// 顶部内边距（动态计算）
pt-[calc(theme(spacing.16)+theme(spacing.20))]    // 移动端：16+20=144px
sm:pt-[calc(theme(spacing.16)+theme(spacing.32))] // 小屏：16+32=192px
```

### 容器宽度策略

```
移动端（< 640px）:
┌──────────────────────────┐
│ px-6 (24px padding)      │
│                          │
│  [内容区域]              │
│                          │
└──────────────────────────┘
width: 100% - 48px

小屏（640px - 768px）:
┌────────────────────────────────┐
│        max-w-[40rem]           │
│        (640px container)       │
└────────────────────────────────┘

中屏（768px - 1024px）:
┌──────────────────────────────────────┐
│          max-w-[48rem]               │
│          px-8 (32px padding)         │
│          (768px container)           │
└──────────────────────────────────────┘

大屏（1024px - 1280px）:
┌────────────────────────────────────────────┐
│            max-w-[64rem]                   │
│            (1024px container)              │
└────────────────────────────────────────────┘

超大屏（1280px+）:
┌──────────────────────────────────────────────────┐
│              max-w-[80rem]                       │
│              (1280px container)                  │
└──────────────────────────────────────────────────┘
```

---

## 性能优化

### 1. 图片优化

#### WebP 格式优先

```jsx
<picture>
  <source srcSet="image@2xl.webp" type="image/webp" />  {/* 优先 */}
  <source srcSet="image@2xr.png" type="image/png" />    {/* 降级 */}
  <img src="image@2xl.webp" />
</picture>
```

**性能对比：**

| 格式 | 文件大小 | 压缩率 | 浏览器支持 |
|------|---------|--------|-----------|
| PNG | 100% | - | 100% |
| JPEG | 40-60% | 中等 | 100% |
| WebP | 25-35% | 高 | 95%+ |
| AVIF | 20-30% | 极高 | 70%+ |

#### 异步解码

```jsx
<img
  decoding="async"   // 图片在后台解码，不阻塞主线程
  loading="lazy"     // 懒加载（视口外的图片延迟加载）
/>
```

#### 预留布局空间

```jsx
<img
  width="1938"
  height="1774"   // 预留空间，防止布局偏移（CLS）
  alt=""          // 装饰性图片使用空 alt
/>
```

### 2. CSS 优化

#### 使用 GPU 加速属性

```css
/* ✅ GPU 加速 */
transform: translateY(-2px);
opacity: 0.5;
filter: blur(10px);

/* ❌ 触发重排 */
top: -2px;
visibility: hidden;
box-shadow: 0 0 10px black;
```

#### 避免昂贵的属性

```css
/* ❌ 性能差 */
box-shadow: 0 0 100px 50px rgba(0,0,0,0.5);  /* 模糊半径过大 */
filter: blur(50px);                           /* 模糊半径过大 */
backdrop-filter: blur(20px);                  /* 非常昂贵 */

/* ✅ 性能优化 */
box-shadow: 0 2px 8px rgba(0,0,0,0.2);       /* 适中模糊 */
filter: blur(10px);                           /* 适中模糊 */
/* 仅在必要时使用 backdrop-filter */
```

#### 使用 will-change（谨慎）

```css
/* 动画元素提前声明 */
.animated-element {
  will-change: transform, opacity;
}

/* 动画结束后移除 */
.animated-element.done {
  will-change: auto;
}
```

### 3. 渲染优化

#### 减少重绘/重排

```jsx
// ✅ 一次性修改多个样式
element.style.cssText = 'transform: translateY(-2px); opacity: 1;';

// ❌ 多次修改触发多次重排
element.style.transform = 'translateY(-2px)';
element.style.opacity = '1';
```

#### 使用 CSS Containment

```css
.hero-section {
  contain: layout style paint;  /* 隔离布局计算 */
}
```

### 4. JavaScript 优化

#### 使用 requestAnimationFrame

```typescript
// ✅ 与浏览器刷新率同步
function animate() {
  // 动画逻辑
  requestAnimationFrame(animate);
}
requestAnimationFrame(animate);

// ❌ 可能与刷新率不同步
setInterval(() => {
  // 动画逻辑
}, 16); // 约 60fps，但不精确
```

#### 防抖/节流滚动事件

```typescript
// 节流：限制执行频率
function throttle(fn: Function, delay: number) {
  let last = 0;
  return (...args: any[]) => {
    const now = Date.now();
    if (now - last >= delay) {
      last = now;
      fn(...args);
    }
  };
}

window.addEventListener('scroll', throttle(() => {
  // 滚动处理
}, 100));
```

---

## 可视化图解

### 1. 层叠顺序图

```
┌─────────────────────────────────────────┐
│ z-index: auto (内容层)                  │  ← 文字、按钮
│ ├── h1 标题                             │
│ ├── p 描述                              │
│ └── button CTA                          │
├─────────────────────────────────────────┤
│ z-index: -10 (装饰层 3)                 │  ← SVG 彩色渐变
│ mix-blend-color-burn                    │
├─────────────────────────────────────────┤
│ z-index: -10 (装饰层 2)                 │  ← 径向渐变遮罩
│ radial-gradient                         │
├─────────────────────────────────────────┤
│ z-index: -10 (装饰层 1)                 │  ← 电路板图片
│ ├── 左侧电路板                          │
│ ├── 右侧电路板（镜像）                  │
│ └── Canvas 连接线                       │
├─────────────────────────────────────────┤
│ bg-gray-50 (基础背景)                   │  ← 基础色
└─────────────────────────────────────────┘
```

### 2. 响应式布局流

```
移动端 (< 640px)
┌──────────────────┐
│   [Logo]         │
│                  │
│   Title (30px)   │
│   Description    │
│   [Button 1]     │
│   [Button 2]     │
└──────────────────┘

小屏 (640px - 768px)
┌────────────────────────┐
│      [Logo]            │
│                        │
│   Title (40px)         │
│   Description          │
│  [Button 1] [Button 2] │
└────────────────────────┘

大屏 (1024px+)
┌────────────────────────────────┐
│         [Logo]                 │
│                                │
│      Title (60px)              │
│      Description               │
│   [Button 1] [Button 2]        │
└────────────────────────────────┘
```

### 3. 按钮层次结构

```
视觉层次（从上到下）:

─────────────────────────────  ← 文字内容
─────────────────────────────  ← ring-1 描边
╔═════════════════════════╗
║ after 伪元素            ║  ← mix-blend-overlay
║ 白色渐变 10%            ║     (46%-54% 高光带)
╚═════════════════════════╝
╔═════════════════════════╗
║ before 伪元素           ║  ← 白色渐变 20%
║ 悬停时 opacity 0→1      ║     (整体覆盖)
╚═════════════════════════╝
▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓  ← bg-purple-500 基础色
═════════════════════════  ← shadow（内阴影+外阴影）
```

### 4. 径向渐变分布

```
圆心位置: 50% 22.26% (水平居中，垂直偏上)
              ↓
        ┌─────●─────┐
       ╱             ╲
      ╱  8.98%       ╲  ← 起始色 (40% 透明)
     │   gray-50/0.4  │
     │                │
     │   45.99%       │  ← 结束色 (100% 不透明)
     │   gray-50      │
     │                │
      ╲              ╱
       ╲            ╱
        ╲          ╱
         └────────┘
```

### 5. 镜像变换可视化

```
原始状态:
┌────────────┐                    ┌────────────┐
│   [图片]   │ right: 50%         │            │
│  ┌──┐      │ origin: default    │            │
│  └──┘      │ scale: 1           │            │
└────────────┘                    └────────────┘
      ↑                                  ↑
  左侧容器                           右侧容器

镜像状态:
┌────────────┐                    ┌────────────┐
│   [图片]   │ right: 50%         │   [片图]   │ right: 50%
│  ┌──┐      │ origin: default    │      ┌──┐  │ origin: right
│  └──┘      │ scale: 1           │      └──┘  │ scale: -1
└────────────┘                    └────────────┘
      ↑                                  ↑
  左侧容器                      右侧容器（围绕右边缘翻转）

最终效果（对称）:
┌────────────┬────────────┐
│  ┌──┐      │      ┌──┐  │
│  │  │      │      │  │  │  ← 完美对称
│  └──┘      │      └──┘  │
└────────────┴────────────┘
```

---

## 实战技巧

### 1. 快速调试层叠顺序

```css
/* 临时添加边框查看层次 */
.hero-section * {
  outline: 1px solid red !important;
}

/* 查看特定层级 */
.hero-section [class*="z-"] {
  outline: 2px solid blue !important;
}
```

### 2. 响应式测试

```jsx
// 使用 TailwindCSS 响应式指示器
<div className="fixed bottom-0 left-0 p-2 bg-black text-white text-xs z-50">
  <span className="sm:hidden">XS</span>
  <span className="hidden sm:inline md:hidden">SM</span>
  <span className="hidden md:inline lg:hidden">MD</span>
  <span className="hidden lg:inline xl:hidden">LG</span>
  <span className="hidden xl:inline">XL</span>
</div>
```

### 3. 性能监控

```typescript
// 测量渲染性能
const observer = new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    console.log(entry.name, entry.startTime);
  }
});
observer.observe({ entryTypes: ['measure', 'paint'] });

// 标记关键时刻
performance.mark('hero-start');
// ... 渲染 Hero
performance.mark('hero-end');
performance.measure('hero-render', 'hero-start', 'hero-end');
```

### 4. 可访问性检查

```jsx
// 装饰性图片
<img src="decoration.png" alt="" aria-hidden="true" />

// Canvas 动画
<canvas aria-hidden="true" />

// SVG 路径定义
<svg width="0" height="0" aria-hidden="true">
  <path d="..." />
</svg>

// 按钮
<a href="/signup" aria-label="开始免费使用">
  Start building for free
</a>
```

### 5. 暗色模式适配

```jsx
// TailwindCSS 暗色模式
<div className="
  bg-gray-50
  dark:bg-gray-900

  text-gray-950
  dark:text-gray-50

  bg-[radial-gradient(...gray.50...)]
  dark:bg-[radial-gradient(...gray.900...)]
">
```

### 6. 动画性能优化

```css
/* 使用 transform 替代 position */
/* ❌ 性能差 */
.button:hover {
  top: -2px;
}

/* ✅ 性能好 */
.button:hover {
  transform: translateY(-2px);
}

/* 使用 opacity 替代 visibility */
/* ❌ 触发重排 */
.overlay {
  visibility: hidden;
}

/* ✅ 只触发重绘 */
.overlay {
  opacity: 0;
  pointer-events: none;
}
```

### 7. 渐变调试工具

```jsx
// 可视化渐变停止点
<div className="relative h-20 bg-gradient-to-r from-purple-500 via-yellow-400 to-cyan-400">
  <div className="absolute top-0 left-[8.98%] h-full w-0.5 bg-red-500" />
  <div className="absolute top-0 left-[45.99%] h-full w-0.5 bg-red-500" />
  <span className="absolute top-0 left-[8.98%] text-xs">8.98%</span>
  <span className="absolute top-0 left-[45.99%] text-xs">45.99%</span>
</div>
```

### 8. 混合模式对比

```jsx
// 快速对比不同混合模式
const blendModes = [
  'normal', 'multiply', 'screen', 'overlay',
  'darken', 'lighten', 'color-dodge', 'color-burn',
  'hard-light', 'soft-light', 'difference', 'exclusion'
];

<div className="grid grid-cols-4 gap-4">
  {blendModes.map(mode => (
    <div key={mode} className="relative h-40 bg-white">
      <div
        className="absolute inset-0 bg-gradient-to-br from-purple-500 to-cyan-400"
        style={{ mixBlendMode: mode }}
      />
      <span className="absolute bottom-2 left-2 text-xs">{mode}</span>
    </div>
  ))}
</div>
```

---

## 总结

### 核心技术点

1. **层叠上下文管理**：`isolate` + `z-index` 控制层级
2. **对称布局**：`origin-right` + `-scale-x-100` 镜像
3. **渐变遮罩**：径向渐变创造视觉焦点
4. **混合模式**：`mix-blend-color-burn` 融合背景
5. **响应式排版**：多级断点精确控制
6. **伪元素层叠**：`before` + `after` 创造深度
7. **性能优化**：WebP + 异步解码 + GPU 加速

### 最佳实践

- ✅ 使用 `isolate` 隔离层叠上下文
- ✅ 优先使用 WebP 格式图片
- ✅ 响应式图片预留布局空间
- ✅ 装饰性元素使用 `aria-hidden="true"`
- ✅ 动画使用 `transform` 和 `opacity`
- ✅ 使用 CSS 变量支持主题切换
- ✅ 混合模式增强视觉效果

### 避免的陷阱

- ❌ 不要在多个容器间混用 `z-index`
- ❌ 不要使用过大的模糊半径（< 20px）
- ❌ 不要硬编码颜色值（使用 CSS 变量）
- ❌ 不要在动画中使用 `top/left`（使用 `transform`）
- ❌ 不要过度使用 `backdrop-filter`（性能昂贵）
- ❌ 不要忘记设置图片 `width` 和 `height`

---

## 下一步

现在您已经深入理解了 Hero 布局的所有技巧，接下来我们将：

1. **创建 `HeroSection` 组件**：基于您的项目技术栈
2. **集成到大屏系统**：适配现有设计语言
3. **添加交互动画**：Canvas 连接线、悬停效果
4. **暗色模式支持**：使用现有 CSS 变量
5. **性能优化**：懒加载、预加载关键资源

准备好进入实践阶段了吗？
